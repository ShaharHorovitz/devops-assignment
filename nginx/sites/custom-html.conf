# Port 8080 - serves the custom HTML page with rate limiting
server {
    listen 8080;
    server_name _;

    root /var/www/html;
    index index.html;

    location / {
        limit_req zone=req_limit burst=10 nodelay;
        try_files $uri $uri/ =404;
    }
}
